apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: welcome-task
spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        echo "Welcome to tekton pipelines"